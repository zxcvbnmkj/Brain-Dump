# David
## 前端
### `lang='pug'``
简洁的缩进语法 替代 HTML 的标签闭合结构

## 后台
### 项目结构
```
project-root/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com.machine.platform/
│   │   │       ├── Application.java          # Spring Boot 启动类
│   │   │       ├── common/                   # 通用组件、工具类
│   │   │       ├── controller/               # 控制层 - REST API
│   │   │       ├── entity/                   # 实体类 - 数据模型
│   │   │       ├── mapper/                   # 数据访问层 - MyBatis Mapper
│   │   │       └── exceps/                   # 自定义异常处理
│   │   └── resources/
│   │       ├── mappings/                     # MyBatis XML 映射文件
│   │       ├── static/                       # 静态资源 (CSS, JS, images)
│   │       └── application.properties        # 应用配置文件
│   └── test/
│       ├── java/                             # 测试源代码
│       └── resources/                        # 测试资源文件
├── target/                          # 🔥 Maven 构建输出目录，不纳入版本控制，可删除后重新生成
│   ├── classes/                              # 编译后的 .class 文件
│   ├── generated-sources/                    # 自动生成的源代码
│   ├── surefire-reports/                     # 单元测试报告
│   └── *.jar                                # 打包后的可执行 JAR
└── pom.xml                                  # 📦 Maven 项目配置依赖管理
```
